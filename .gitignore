# Python
*.pyc
__pycache__/
*.pyo
*.pyd
.Python
*.so
.coverage
.pytest_cache/
*.egg-info/
dist/
build/

# Virtual Environment
venv/
env/
ENV/

# Google Cloud Credentials —— 用裸 *.json 作凭证安全网:
# GCP 控制台下载的 service-account 密钥默认随机命名(如 my-project-abc123.json /
# firebase-adminsdk-xxx.json),无关键词可匹配,只有 *.json 兜底才挡得住。本仓当前
# 无需追踪任何 JSON fixture(git ls-files 为空),故安全性优先;确需追踪某个 JSON
# 时用 `git add -f <file>` 显式豁免。下列具名行仅作自文档。
*.json
# 具名凭证(被上面的 *.json 覆盖,保留作说明): google-credentials.json / serviceKey.json
.vscode/google-credentials.json
.vscode/serviceKey.json

# Application specific
uploads/*
!uploads/.gitkeep
output/*
!output/.gitkeep

# Environment variables
.env
.env.local
.env.production

# Logs
*.log
logs/

# OS specific
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
*.temp
get-pip.py
.venv*/
